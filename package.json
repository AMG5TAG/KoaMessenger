{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "start:desktop": "concurrently --names API,WEB,desktop --prefix-colors yellow,green,cyan \"pnpm --filter @workspace/api-server run dev\" \"pnpm --filter @workspace/koa-messenger run dev\" \"sleep 6 && pnpm --filter @workspace/koa-messenger-desktop run dev\""
  },
  "private": true,
  "devDependencies": {
    "@esbuild/darwin-arm64": "0.27.3",
    "concurrently": "^9.2.1",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
